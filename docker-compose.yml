services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: sikto
      POSTGRES_PASSWORD: sikto
      POSTGRES_DB: sikto
    ports:
      - "5432:5432"
    volumes:
      - sikto_pgdata:/var/lib/postgresql/data
volumes:
  sikto_pgdata:

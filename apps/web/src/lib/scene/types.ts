// The scene spec lives in the shared @sikto/scene-kit package so the live player
// and the Remotion exporter render through the exact same types and components.
// Re-exported here to keep existing `@/lib/scene/types` imports working.
export * from '@sikto/scene-kit';

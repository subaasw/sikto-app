// The scene spec + render inputs live in the shared @sikto/scene-kit package so
// the Remotion exporter and the live web player stay byte-for-byte in sync.
// Re-exported here to keep existing `./schema` imports working.
export * from '@sikto/scene-kit';
